import 'package:flutter/material.dart';

class AppTheme {
  static const primaryColor = Color(0xFF003366); // Deep navy blue
  static const accentColor = Color(
    0xFFE63946,
  ); // Red accent (can be green/gold)
  static const backgroundColor = Color(0xFFF4F4F4); // Light grey
  static const cardColor = Colors.white;
  static const navBarBackgroundColor = Colors.white;
  static const navBarUnselectedColor = Colors.grey;

  static const cardTextStyle = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w600,
    color: Colors.black87,
  );

  static const titleTextStyle = TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.bold,
    color: primaryColor,
  );

  static const subtitleTextStyle = TextStyle(
    fontSize: 16,
    color: Colors.black54,
  );
}

import 'package:flutter/material.dart';

class AppTheme {
  static const primaryColor = Color(0xFF003366); // Deep navy blue
  static const accentColor = Color(
    0xFFFFD700,
  ); // Red accent (can be green/gold)
  static const backgroundColor = Color(0xFFF4F4F4); // Light grey
  static const cardColor = Colors.white;
  static const navBarBackgroundColor = Colors.white;
  static const navBarUnselectedColor = Colors.grey;

  static const cardTextStyle = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w600,
    color: Colors.black87,
  );

  static const titleTextStyle = TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.bold,
    color: primaryColor,
  );

  static const subtitleTextStyle = TextStyle(
    fontSize: 16,
    color: Colors.black54,
  );
}